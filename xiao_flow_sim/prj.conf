# Logs por la consola USB CDC-ACM, que la propia placa habilita e inicializa.
CONFIG_LOG=y

# Bluetooth LE: rol periferico + servidor GATT.
CONFIG_BT=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_DEVICE_NAME="XIAO-FLOW"
CONFIG_BT_MAX_CONN=1
